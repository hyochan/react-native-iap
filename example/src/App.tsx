import React from 'react';
import AppNavigator from './navigation/AppNavigator';

/**
 * Main App Component
 * Uses the AppNavigator to handle all screen navigation
 */
function App(): React.JSX.Element {
  return <AppNavigator />;
}

export default App;
